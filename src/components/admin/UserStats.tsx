import React from "react";
import { ContainerWithRows } from "../styled-components/Reusables";

interface Props {}

const UserStats = (props: Props) => {
    return <ContainerWithRows></ContainerWithRows>;
};

export default UserStats;
