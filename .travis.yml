language: bash

sudo: required

services:
  - docker

script: 
  - docker build -t adamzopcsak/dojotrackerfe .
  - echo "$DOCKER_PASSWORD" | docker login --username $DOCKER_USERNAME --password-stdin
  - docker push adamzopcsak/dojotrackerfe

notification:
  email:
      recipients:
          - adamzopcsak@gmail.com
          - kgalmos@gmail.com
          - gyula.g.hodosi@gmail.com
      on_success: always
      on_failure: always
    